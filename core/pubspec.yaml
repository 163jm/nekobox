name: core
description: NekoBox Flutter shared core - proxy parsing, config building, process control
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  yaml: ^3.1.2
  path: ^1.9.0
  path_provider: ^2.1.3
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  sqlite3_flutter_libs: ^0.5.0
  archive: ^3.4.0
  file_picker: ^8.1.2

dev_dependencies:
  lints: ^4.0.0
